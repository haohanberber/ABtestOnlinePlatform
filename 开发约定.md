# 开发约定

本项目采用“文档先行、代码同步”的维护策略：

1. 每次改动前先阅读 `页面功能说明.md`。
2. 新增/修改功能后，必须同步更新对应说明文档。
3. 当前存在两套代码组织：
   - 运行入口：`app.js`
   - 模块化目录：`features/`
4. 任意一侧改动，都要同步另一侧，防止逻辑漂移。
5. 若规则发生变化，同时更新：
   - `.cursor/rules/page-change-governance.mdc`
   - `页面功能说明.md`
   - 本文件 `开发约定.md`
