---
description: 页面功能变更流程与双代码入口同步规范
alwaysApply: true
---

# 页面变更治理规则

每次新增或修改页面功能时，必须遵守：

1. 先阅读 `页面功能说明.md`，确认当前功能边界与交互约定。
2. 先确定影响范围，再动代码，避免回归问题。
3. 变更后同步更新文档（至少更新 `页面功能说明.md`）。
4. 若改动了 `app.js`，需同步对应 `features/*`；若改动了 `features/*`，需同步 `app.js`。
5. 完成后至少自检：上传、预览弹窗、分桶统计、导出 Excel、实验计算。
